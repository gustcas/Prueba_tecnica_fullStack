// Firebase Messaging service worker placeholder.
// This file must exist at the root (/) so that
// getMessaging() can register the default service worker.

// You can extend this to handle background notifications.
self.addEventListener('push', () => {
  // No-op for now.
});

